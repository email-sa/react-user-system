import { Form, Input, Button, Select } from "antd";

const query = [
    { key: "name", type: "Input", label: "姓名", placeholder: "请输入姓名" },
    { key: "name", type: "Input", label: "性别", placeholder: "请选择" }
];

export { query };
